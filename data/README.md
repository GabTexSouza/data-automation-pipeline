# Dados

A pasta `data/raw` será usada para armazenar os arquivos mensais de entrada.

A pasta `data/processed` será usada para armazenar a base consolidada e tratada.

Os arquivos de dados gerados localmente não serão versionados no GitHub para manter o repositório leve.